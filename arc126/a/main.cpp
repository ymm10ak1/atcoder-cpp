// Make 10
#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define repi(i, s, n) for(int i=(int)(s); i<(int)(n); ++i)
#define rep(i, n) repi(i, 0, n)
#define rrepi(i, n, s) for(int i=n-1; i>=s; --i)
#define repli(i, s, n) for(ll i=(s); i<(ll)(n); ++i)
#define repl(i, n) repli(i, 0LL, n)
#define all(x) x.begin(), x.end()
#define rall(x) x.rbegin(), x.rend()
#define el '\n'
const int INF = 1e9;
const ll LINF = 1e18;

// TODO: 難しくて解けないので一旦放置
int main(){
    // int t;
    // cin >> t;
    // vector<ll> ans;
    // rep(i, t){
    //     ll n2, n3, n4;
    //     cin >> n2 >> n3 >> n4;
    //     if((n2==0 && n3==0) || (n2==0 && n4==0) || (n3==0 && n4==0 && n2<5)){
    //         ans.push_back(0);
    //         continue;
    //     }else if(n2<2 && n3<2 && n4<2){
    //         ans.push_back(0);
    //         continue;
    //     }
    //     ll sum = 0;
    //     ans.push_back(sum);
    // }
    // rep(i, ans.size()){
    //     cout << ans[i] << el;
    // }
    return 0;
}